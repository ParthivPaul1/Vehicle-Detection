In this section, different versions of YOLO models are trained on IRUVD dataset and those are run on Kaggle environment utilising NVDIA P100 gpu and fine tuned to get the best training results, which can be used to train the model in adverse conditions (different colorspaces).
